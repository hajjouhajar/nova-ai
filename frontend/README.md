
  # Créer une interface utilisateur

  This is a code bundle for Créer une interface utilisateur. The original project is available at https://www.figma.com/design/aPtvY8tj5dimYjt51lmR7A/Cr%C3%A9er-une-interface-utilisateur.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  